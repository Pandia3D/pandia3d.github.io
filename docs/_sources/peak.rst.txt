=====================
Pandia Peak Endpoints
=====================

These are the endpoints of the Pandia Peak software. The endpoints require that the system is activated and set up via GUI.

.. toctree::
   :maxdepth: 2

   peak_endpoints/VolumeStart
   peak_endpoints/VolumeStop
   peak_endpoints/VolumeGet
   peak_endpoints/FlowStart
   peak_endpoints/FlowPause
   peak_endpoints/FlowReset
   peak_endpoints/FlowGet
